{
  "schema_version": 1,
  "logical_id": "brrmmmm.demo.weather",
  "name": "Demo Weather Sidecar",
  "description": "Fetches current temperature from Open-Meteo (no API key required). Demonstrates the brrmmmm host ABI.",
  "abi_version": 3,
  "run_modes": ["managed_polling", "interactive"],
  "state_persistence": "host_persisted",
  "required_env_vars": [],
  "optional_env_vars": [
    {
      "name": "LATITUDE",
      "description": "Decimal latitude of the location to fetch weather for (default: 52.52 — Berlin)"
    },
    {
      "name": "LONGITUDE",
      "description": "Decimal longitude (default: 13.41)"
    }
  ],
  "params": {
    "fields": [
      {
        "key": "location_name",
        "type": "string",
        "required": false,
        "label": "Location name",
        "help": "Human-readable label shown in published output.",
        "default": "Berlin",
        "options": []
      }
    ]
  },
  "capabilities_needed": ["network"],
  "poll_strategy": {
    "kind": "fixed_interval",
    "interval_secs": 300
  },
  "cooldown_policy": {
    "authority": "host_persisted",
    "min_interval_ms": 60000
  },
  "artifact_types": [
    "raw_source_payload",
    "normalized_payload",
    "published_output"
  ]
}
