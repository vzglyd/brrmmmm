{
  "schema_version": 1,
  "logical_id": "brrmmmm.fixture.google_search",
  "name": "Google Search Fixture",
  "description": "Drives google.com through the browser ABI, selects a random TREC topic, submits the query, retrieves result HTML, and publishes structured JSON.",
  "abi_version": 1,
  "run_modes": ["managed_polling"],
  "state_persistence": "host_persisted",
  "required_env_vars": [],
  "optional_env_vars": [],
  "params": {
    "fields": []
  },
  "capabilities_needed": ["browser"],
  "poll_strategy": {
    "kind": "fixed_interval",
    "interval_secs": 60
  },
  "cooldown_policy": {
    "authority": "host_persisted",
    "min_interval_ms": 60000
  },
  "artifact_types": [
    "published_output"
  ]
}
